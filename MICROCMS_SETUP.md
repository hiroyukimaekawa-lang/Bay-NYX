# microCMS 操作マニュアル（Bay NYX ホームページ）

このマニュアルは、**ホームページの写真や文章を、店舗スタッフの方が自分で変更する**ためのものです。
GitHubやプログラムの知識は必要ありません。microCMSの管理画面だけで作業できます。

管理画面はこちらです。

https://l9pawk28o1.microcms.io/apis/baynyx

---

## 目次

1. [変更できるもの](#1-変更できるもの)
2. [最初に一度だけ必要な準備（フィールド追加）](#2-最初に一度だけ必要な準備フィールド追加)
3. [通常キャストを登録する](#3-通常キャストを登録する)
4. [期間限定キャストを登録する](#4-期間限定キャストを登録する)
5. [料理を登録する](#5-料理を登録する)
6. [写真を変更する](#6-写真を変更する)
7. [並び順を変える](#7-並び順を変える)
8. [一時的に非表示にする](#8-一時的に非表示にする)
9. [反映されるまでの時間](#9-反映されるまでの時間)
10. [【重要】APIキーの再発行について](#10-重要apiキーの再発行について)
11. [【技術者向け】Cloudflare Pages の環境変数設定](#11-技術者向けcloudflare-pages-の環境変数設定)
12. [困ったときは](#12-困ったときは)

---

## 1. 変更できるもの

microCMSから変更できるのは次の3種類です。

| 種類 | 表示される場所 |
| --- | --- |
| 通常キャスト | トップページの「Golden Cards」のカード一覧 |
| 期間限定キャスト | トップページの「Special Guest」欄 |
| 料理 | Serviceページの「料理メニュー」 |

料金表・住所・電話番号・営業時間・コンセプト文章は、microCMSでは変更できません。
これらの変更が必要なときは制作担当者へご連絡ください。

---

## 2. 最初に一度だけ必要な準備（フィールド追加）

現在のmicroCMSには `name` `photo` `description` `instagram_url` の4つしかありません。
下の表のフィールドを **一度だけ** 追加してください。追加後はずっと使えます。

### 手順

1. 管理画面を開く → 右上の **「API設定」** をクリック
2. **「APIスキーマ」** をクリック
3. **「フィールドを追加」** を押して、下の表のとおりに入力
4. すべて追加できたら **「変更を適用」** を押す

### 追加するフィールド一覧

| 表示名 | フィールドID | 種類 | 必須 | 内容 |
| --- | --- | --- | --- | --- |
| 種別 | `content_type` | セレクトフィールド | 必須 | 下の「選択肢」を参照 |
| 表示順 | `sort_order` | 数字 | 任意 | 小さい数字から順に表示 |
| 表示する | `is_visible` | 真偽値 | 任意 | オフにすると非表示 |
| SNS表示名 | `social_label` | テキストフィールド | 任意 | `Instagram` や `TikTok` など |
| カード数字 | `card_rank` | テキストフィールド | 任意 | `A` `K` `Q` `J` `10` `9` など |
| カード記号 | `card_symbol` | テキストフィールド | 任意 | `♠` `♥` `♦` `♣` |
| 動画URL | `video_url` | テキストフィールド | 任意 | Cloudinaryの動画URLを貼り付けます（→ [第6章](#6-写真を変更する)） |

> **`content_type` の選択肢**（セレクトフィールドの「選択肢」欄に、1行ずつ入力）
>
> ```
> cast
> special_guest
> food
> ```
>
> 「選択肢から選べる数」は **1つ** のままにしてください。

### 既存フィールド（すでにあるもの・変更不要）

| 表示名 | フィールドID | 内容 |
| --- | --- | --- |
| 名前 | `name` | キャスト名または料理名 |
| 説明文 | `description` | 紹介文・料理の説明 |
| 写真 | `photo` | 写真 |
| SNS URL | `instagram_url` | InstagramまたはTikTokのURL |

> ⚠️ **フィールドIDは、表の英数字と1文字も違わないように入力してください。**
> 1文字でも違うとホームページに反映されません。

---

## 3. 通常キャストを登録する

トップページの「Golden Cards」に表示されるカードです。

1. 管理画面で **「追加」** ボタンを押す
2. 次のように入力する

| 項目 | 入力例 | 説明 |
| --- | --- | --- |
| 種別 (`content_type`) | **cast** | ← 必ず `cast` を選ぶ |
| 名前 | NICOLE | カードに表示される名前 |
| 説明文 | クールで知的な雰囲気 | カードの裏面に出る一言 |
| 写真 | （画像をアップロード） | カード裏面の背景写真 |
| SNS URL | https://www.instagram.com/... | 押したときに開くリンク |
| SNS表示名 | Instagram | ボタンに出る文字 |
| カード数字 | A | トランプの数字 |
| カード記号 | ♠ | トランプのマーク |
| 表示順 | 1 | 小さいほど先に出る |
| 表示する | オン | |

3. 右上の **「公開」** を押す

これでトップページにカードが1枚増えます。カードを増やすたびに同じ手順を繰り返してください。

> 💡 カード数字・カード記号を空欄にしても表示はされますが、
> 自動で割り当てられるため、**できるだけ入力する**ことをおすすめします。

---

## 4. 期間限定キャストを登録する

「Special Guest」欄に表示されるカードです。手順は通常キャストと同じですが、
**種別 (`content_type`) を `special_guest` にする**点だけが違います。

| 項目 | 入力 |
| --- | --- |
| 種別 (`content_type`) | **special_guest** |

その他の項目は通常キャストと同じです。

> ⚠️ 期間限定キャストが **0件になると、「Special Guest Card を見る」ボタンごと消えます。**
> これは正常な動作です。また登録すれば自動的に復活します。

> 💡 現在すでに登録されている3件（KEI / NICOLE / ROMA）は種別が空欄ですが、
> **種別が空欄のものは自動的に「期間限定キャスト」として扱われます**ので、
> 今までどおり表示され続けます。通常キャストに移したい場合は、種別を `cast` に変更してください。

---

## 5. 料理を登録する

Serviceページの「料理メニュー」に表示されます。

1. 管理画面で **「追加」** ボタンを押す
2. 次のように入力する

| 項目 | 入力例 |
| --- | --- |
| 種別 (`content_type`) | **food** |
| 名前 | お好み焼き |
| 説明文 | ふんわり 香ばしく |
| 写真 | （料理の画像をアップロード） |
| 表示順 | 1 |
| 表示する | オン |

3. **「公開」** を押す

> ⚠️ **料理を1件でも登録すると、今ホームページに出ている料理一覧はすべてmicroCMSの内容に置き換わります。**
> 途中まで登録した状態だと、登録した分しか表示されません。
> 料理をmicroCMS管理に切り替えるときは、**11品すべてを登録してから公開**してください。
>
> 💡 **料理名を下記のとおりに入力すれば、写真をアップロードしなくても
> 今の写真がそのまま引き継がれます。** まずは全11品を名前だけ登録して公開し、
> あとから写真を1品ずつ差し替えていくのが安全です。
>
> 現在ホームページに載っている料理は次の11品です。
> お好み焼き / 焼きそば / チキンライス / チャーハン / フライチキン（要予約） /
> ヒレステーキ（要予約） / 自家製あんバターサンド / お茶漬け / フライポテト /
> プチピザ / 日替わり レーズンバター & チョコ

---

## 6. 写真を変更する

1. 管理画面で、変更したいキャストまたは料理をクリック
2. **「写真」** 欄の画像をクリックし、新しい画像を選ぶ
3. 右上の **「公開」**（すでに公開済みなら「更新」）を押す

これだけでホームページの写真が入れ替わります。**GitHubの操作は一切不要です。**

### 写真のおすすめサイズ

| 用途 | おすすめ |
| --- | --- |
| キャスト写真 | 縦長（例: 800×1200ピクセル程度） |
| 料理写真 | 横長〜正方形（例: 1200×900ピクセル程度） |

写真は自動的に枠に合わせて切り抜かれます。**顔や料理が中央に来るように撮影・トリミング**してください。

### 動画を変更する（重要）

**動画はmicroCMSの「動画URL (`video_url`)」から変更できます。GitHubの操作は不要です。**

動画ファイルそのものは **Cloudinary**（動画置き場）に保管し、
microCMSにはその **URLだけ** を登録します。
（microCMSの無料プランのままお使いいただくための構成です。
 microCMSに動画ファイルをアップロードしないでください。）

#### 動画の変更手順

1. **Cloudinary** へログインし、新しい動画をアップロードする
2. アップロードした動画の **Delivery URL（配信URL）** をコピーする
3. **microCMS** を開く
4. 変更したい **キャストを開く**
5. **「動画URL (`video_url`)」** の欄に、コピーしたURLを貼り付ける
6. **「公開」**（すでに公開済みなら「更新」）を押す
7. ホームページを開き直すと、新しい動画に切り替わります

> 💡 URLは `https://res.cloudinary.com/` で始まるものを貼り付けてください。
> 末尾が `.mp4` の形式が、パソコン・スマホのどちらでも確実に再生できます。

#### 動画をやめて写真に戻したいとき

**「動画URL (`video_url`)」の欄を空にして、更新してください。**
動画URLが空になると、microCMSに登録した **写真 (`photo`)** の表示に戻ります。

> ⚠️ 写真も動画URLも両方とも空の場合は、
> ホームページ側にあらかじめ設定されている動画・画像が表示されます。

#### 現在、動画が設定されているキャスト

| キャスト名 | 動画 |
| --- | --- |
| NICOLE | Cloudinary（`NICOLE.mp4`） |
| ROMA | Cloudinary（`ROMA.mp4`） |
| AYA | Cloudinary（`aya.mp4`） |
| RAIKA（ライカ） | Cloudinary（`ryca.mp4`） |
| HONEY（ハニー） | Cloudinary（`Baynyx-honey.mp4`） |
| REIKA（レイカ） | Cloudinary（`bay-nyx.mp4`） |

> 上記はホームページ側に設定されている動画です。
> microCMSの「動画URL」を入力すると、**そちらが優先** して表示されます。

### microCMSに写真を登録しなかった場合

**いま表示されている画像・動画がそのまま引き継がれます。**
microCMSの「名前」が、ホームページに書かれているキャスト名・料理名と
一致していれば、自動的に今の画像・動画が使われます。

つまり、次のような使い方ができます。

| やりたいこと | 操作 |
| --- | --- |
| 写真はそのままで、紹介文だけ変えたい | 写真欄は空のまま、説明文だけ入力する |
| 並び順だけ変えたい | 写真欄は空のまま、`sort_order` だけ入力する |
| 写真も変えたい | 写真欄に新しい画像をアップロードする |

> 💡 名前が一致しないと引き継がれず、写真のないカードになります。
> **名前はホームページの表記どおりに入力**してください（大文字・小文字の違いは無視されます）。

### キャストの画像が決まる順番

1. microCMSの **動画URL** (`video_url`) … 入力されていれば最優先
2. microCMSの**写真** (`photo`)
3. ホームページに設定されている**動画**（名前が一致するもの／Cloudinary）
4. ホームページに設定されている**既存の画像**（名前が一致するもの）

> microCMSからデータを取得できなかったときは、
> ホームページに書かれている内容がそのまま表示されます。

---

## 7. 並び順を変える

**表示順 (`sort_order`)** の数字を変えるだけです。

| 表示順 | 結果 |
| --- | --- |
| 1 | 一番最初に表示 |
| 2 | 2番目に表示 |
| 3 | 3番目に表示 |
| （空欄） | 一番最後に表示 |

数字を変えたら **「更新」** を押してください。

> 💡 あとから間に差し込めるように、`10` `20` `30` …のように
> **10刻みで付けておく**と後の変更がラクです。

---

## 8. 一時的に非表示にする

削除せずに一時的に隠したいときは、**表示する (`is_visible`) をオフ**にして「更新」を押します。

| 設定 | 結果 |
| --- | --- |
| オン | 表示される |
| オフ | 表示されない |
| （未設定） | 表示される |

また表示したくなったら、オンに戻すだけです。

---

## 9. 反映されるまでの時間

公開・更新してから、ホームページに反映されるまで **最大で5分程度**かかります。
すぐに反映されない場合は、少し待ってからブラウザを再読み込み（スーパーリロード）してください。

| 端末 | 操作 |
| --- | --- |
| Windows | `Ctrl` + `F5` |
| Mac | `Command` + `Shift` + `R` |

---

## 10. 【重要】APIキーの再発行について

> ⚠️ **これは制作担当者・管理者向けの重要な注意事項です。**

以前のホームページには、microCMSのAPIキーが **ブラウザから誰でも見える状態** で
HTMLの中に直接書かれていました。今回の修正でコードからは削除しましたが、
**GitHubの過去のコミット履歴には残っており、第三者が閲覧できる状態が続いています。**

そのため、次の対応を強く推奨します。

### 推奨する手順

1. microCMS管理画面 → **「サービス設定」** → **「APIキー」** を開く
2. **新しいAPIキーを発行**する
3. 権限を次のとおり設定する

   | 項目 | 設定 |
   | --- | --- |
   | 対象API | **baynyx のみ** |
   | GET | ✅ 許可する |
   | POST / PUT / PATCH / DELETE | ❌ 許可しない |
   | Management API | ❌ 許可しない |

4. Cloudflare Pages の環境変数 `MICROCMS_API_KEY` を新しいキーに更新する（→ 第11章）
5. サイトが正常に表示されることを確認したうえで、**古いキーを削除する**

現時点ではこの再発行は行っていないため、**古いキーは有効なままです。**

---

## 11. 【技術者向け】Cloudflare Pages の環境変数設定

このサイトは Cloudflare Pages にデプロイし、APIキーはサーバー側の
Pages Functions (`functions/api/baynyx.js`) だけが読み取ります。ブラウザには送られません。

### 環境変数

Cloudflare ダッシュボード → 対象のPagesプロジェクト →
**Settings** → **Environment variables** から設定します。

| 変数名 | 値 | 必須 | 備考 |
| --- | --- | --- | --- |
| `MICROCMS_API_KEY` | （microCMSのAPIキー） | **必須** | **Secret（暗号化）として登録する。デフォルト値なし。** |
| `MICROCMS_SERVICE_DOMAIN` | `l9pawk28o1` | 任意 | 未設定ならこの値が使われる |
| `MICROCMS_ENDPOINT` | `baynyx` | 任意 | 未設定ならこの値が使われる |

**Production と Preview の両方**に設定してください。設定後は再デプロイが必要です。

### 動作のしくみ

```
ブラウザ  →  /api/baynyx  →  Pages Functions  →  microCMS
                              （ここにAPIキー）
```

`functions/api/baynyx.js` はファイルの場所がそのままURLになる仕組みのため、
リダイレクト設定（`_redirects` など）は不要です。

### ローカルで確認する方法

```bash
npx wrangler pages dev .
```

`.dev.vars` というファイルをリポジトリ直下に作ると、ローカル用の環境変数を設定できます。

```
MICROCMS_API_KEY=（ローカル確認用のキー）
```

> ⚠️ `.dev.vars` は **絶対にGitにコミットしないでください。**（`.gitignore` に登録済みです）

### 静的サーバーだけで見た場合

```bash
python3 -m http.server 8080
```

この方法では `/api/baynyx` が存在しないため、microCMSの内容は反映されません。
その代わり **HTMLに書かれている既存の内容がそのまま表示されます**（デザイン確認用）。
これは異常ではなく、意図した動作です。

---

## 12. 困ったときは

| 症状 | 確認すること |
| --- | --- |
| 登録したのに表示されない | 「公開」を押したか／`content_type` が正しいか／`is_visible` がオンか |
| 種別を選ぶ欄がない | 第2章のフィールド追加がまだ済んでいません |
| 順番が思ったとおりにならない | `sort_order` の数字を確認（空欄は最後） |
| 更新したのに変わらない | 5分ほど待ってスーパーリロード（第9章） |
| 料理が数品しか出ない | microCMSに登録した分だけが表示されます（第5章の注意を参照） |
| Special Guestのボタンが消えた | `special_guest` の登録が0件になっています |
| 写真の顔が切れる | 中央に寄せてトリミングし直してください（第6章） |

上記で解決しない場合は、次の情報を添えて制作担当者へご連絡ください。

- どのページか（トップ / Service）
- どの登録内容か（名前）
- いつ更新したか
- 使用した端末とブラウザ
