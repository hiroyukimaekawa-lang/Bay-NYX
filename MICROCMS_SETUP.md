# microCMS 操作・運用マニュアル（Bay NYX 公式サイト）

このマニュアルは、**ホームページの料理メニューやスタッフ情報を店舗スタッフ・管理担当者が直接変更する**ための運用ガイドです。
GitHubやプログラミングの知識は不要です。microCMS の管理画面のみで設定完結できます。

---

## 1. 管理画面・システム構成

* **管理画面 URL**: https://l9pawk28o1.microcms.io/

```text
 microCMS (マスターデータ)
   ├─ food-menu (料理メニュー)
   └─ staff (スタッフ Golden Cards)
        │
        ▼ GET (X-MICROCMS-API-KEY / Netlify Functions)
 Netlify Functions (プロキシ)
   ├─ /.netlify/functions/food-menu
   └─ /.netlify/functions/staff
        │
        ▼ 自動反映 (更新から約1〜2分以内)
 Webサイト (https://baynyx.netlify.app/service & index.html)
```

---

## 2. API作成・フィールド定義 (初回のみ設定)

microCMS 管理画面にて以下の **2つの API (リスト形式)** を作成し、それぞれのフィールドを設定します。

### 2-1. 料理メニュー API (`food-menu`)

* **API名**: 料理メニュー
* **エンドポイント**: `food-menu`
* **API形式**: リスト形式

| 表示名 | フィールドID | 種類 | 必須 | 内容・入力例 |
| :--- | :--- | :--- | :--- | :--- |
| 固定識別子 | `key` | テキストフィールド | **必須** | HTML照合キー（例: `ochazuke`, `petit-pizza`） |
| 料理名 | `name` | テキストフィールド | **必須** | 料理の名称（例: お茶漬け, プチピザ） |
| 料理説明 | `description` | テキストフィールド | 任意 | 料理の説明文（例: 〆の一杯に） |
| 料理画像 | `image` | 画像 | **必須** | 料理の写真 |
| 表示順 | `sortOrder` | 数字 | **必須** | 昇順の数値（例: 80, 100, 120...） |
| 表示状態 | `isVisible` | 真偽値 | **必須** | **オン**: サイトに表示 / **オフ**: 非表示 |

> 💡 **既存11料理のキー一覧 (`key`)**:
> `okonomiyaki` (お好み焼き: 10), `yakisoba` (焼きそば: 20), `chicken-rice` (チキンライス: 30), `fried-rice` (チャーハン: 40), `fried-chicken` (フライチキン: 50), `fillet-steak` (ヒレステーキ: 60), `an-butter` (自家製あんバター: 70), `ochazuke` (お茶漬け: 80), `fried-potato` (フライポテト: 90), `petit-pizza` (プチピザ: 100), `raisin-butter` (レーズンバター: 110)

---

### 2-2. スタッフ API (`staff`)

* **API名**: スタッフ
* **エンドポイント**: `staff`
* **API形式**: リスト形式

| 表示名 | フィールドID | 種類 | 必須 | 内容・選択肢 |
| :--- | :--- | :--- | :--- | :--- |
| 固定識別子 | `key` | テキストフィールド | **必須** | HTML照合キー（例: `nicole`, `roma`, `kei`） |
| スタッフ名 | `name` | テキストフィールド | **必須** | スタッフ名（例: NICOLE, ROMA, KEI） |
| 紹介文 | `description` | テキストフィールド | 任意 | プロフィール・一言紹介 |
| 写真 | `image` | 画像 | 任意 | 写真（未設定時は既存動画/画像を維持） |
| カードランク | `cardRank` | テキストフィールド | **必須** | トランプの数字（例: `A`, `K`, `Q`, `J`, `10`, `9`） |
| カードスート | `cardSuit` | セレクトフィールド | **必須** | `heart` (♥), `diamond` (♦), `spade` (♠), `club` (♣) |
| SNS種別 | `socialType` | セレクトフィールド | 任意 | `instagram`, `tiktok`, `none` |
| SNS URL | `socialUrl` | テキストフィールド | 任意 | SNSリンクURL (`https://www.instagram.com/...`) |
| 表示順 | `sortOrder` | 数字 | **必須** | 昇順の数値（例: 10, 20, 30...） |
| 表示状態 | `isVisible` | 真偽値 | **必須** | **オン**: サイトに表示 / **オフ**: 非表示 |

> 💡 **既存スタッフのキー一覧 (`key`)**:
> `baynyx` (10), `nicole` (20), `roma` (30), `kei` (40), `joy` (50), `aya` (60), `raika` (70), `honey` (80), `reika` (90)

---

## 3. コンテンツの追加・編集手順

### 料理を追加したいとき
1. microCMS 管理画面 → **「料理メニュー」** を開く
2. 右上の **「追加」** ボタンを押す
3. キー (`key`), 料理名 (`name`), 写真 (`image`), 表示順 (`sortOrder`), 表示状態 (`isVisible` = オン) を入力
4. 右上の **「公開」** ボタンを押す
5. **約1〜2分以内** に Web サイト上へ自動反映されます。（GitHub コミットや Netlify 再デプロイは不要）

### スタッフを追加したいとき
1. microCMS 管理画面 → **「スタッフ」** を開く
2. 右上の **「追加」** ボタンを押す
3. キー (`key`), 名前 (`name`), カードランク (`cardRank`), カードスート (`cardSuit`), 表示順 (`sortOrder`), 表示状態 (`isVisible` = オン) を入力
4. **「公開」** ボタンを押す

---

## 4. 【重要】非表示（削除）運用ルール

> ⚠️ **重要注意事項**
>
> 既存の料理やスタッフをサイト上から一時的・恒久的に消したい場合は、**microCMS 上でコンテンツ自体を削除しないでください。**
>
> **正しく非表示にする方法:**
> * 表示状態 (`isVisible`) を **オフ (false)** に設定して更新します。
>
> **理由:**
> コンテンツ自体を完全削除すると、システムのフォールバック機能が働き、初期 HTML に記述されているカードが再表示されてしまう可能性があります。必ず **`isVisible = false`** で非表示制御を行ってください。

---

## 5. 並び順 (`sortOrder`) のルール

* `sortOrder` の数字が **小さい順** にサイト上に並びます。
* あとから新しい料理やスタッフを間に入れることができるよう、`10`, `20`, `30`, `40` ... と **10刻み** で設定することを推奨します。
  * 例: お茶漬け (80) と フライポテト (90) の間に新しい料理を入れたい場合は `85` を指定します。
* もし同じ `sortOrder` が指定された場合は、キー (`key`) のアルファベット順で整列されます。

---

## 6. Netlify 環境変数の設定 (管理者向け)

Netlify のダッシュボード → **Site configuration** → **Environment variables** にて以下を設定します。

| 変数名 | 設定値 | 必須 |
| :--- | :--- | :--- |
| `MICROCMS_SERVICE_DOMAIN` | `l9pawk28o1` | **必須** |
| `MICROCMS_API_KEY` | (microCMSのAPIキー) | **必須** |

* API キーはフロントエンドの JavaScript や HTML に直接記載されず、サーバーレス関数 (`/.netlify/functions/`) 内でのみ使用されるため安全です。
* 通信は `GET` リクエストのみ許可され、更新・削除権限は保護されています。
